namespace OOPPractice;

class Program
{
    static void Main(string[] args)
    {
        // Bird flapyBird = new Bird("Gray", 0.5, "White");
        // Console.WriteLine(flapyBird.Speak());
        // flapyBird.BirdMove();
        // flapyBird.Move();
        // flapyBird.FeatherColor
        // flapyBird.Fly();

        // Pet pet = new Pet("Black", 2.5);
        // pet.ToString();
        // Console.WriteLine(pet.Speak());

        // Duck donan = new Duck("Black", 2);
        // donan.ToString();
        // Console.WriteLine(donan.Move());
        // Console.WriteLine(donan.DuckMoving());

        // Point2D point = new Point2D(5, 10);
        // Console.WriteLine(point.ToString());

        Point3D point = new Point3D(5, 10, 15);
        Console.WriteLine(point.ToString());
    }
}
